# ScrapeWork Dashboard (`web/`)

Next.js App Router UI for the job feed, pitch drawer, and asset manager.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Product docs: `../.cursorrules/`.
