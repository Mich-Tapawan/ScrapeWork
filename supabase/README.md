# `supabase/`

Postgres schema and migrations for ScrapeWork.

- Apply `migrations/001_initial_schema.sql` in the Supabase SQL editor or via CLI.
- Keep `.cursorrules/schema.md` in sync when changing tables.
